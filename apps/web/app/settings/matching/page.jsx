import MatchingSettingsPage from "@/features/matching/pages/MatchingSettingsPage";

export default function Page() {
  // 매칭 조건 설정 주소(/settings/matching)를 담당하고, 실제 화면은 features/matching에서 불러옵니다.
  return <MatchingSettingsPage />;
}